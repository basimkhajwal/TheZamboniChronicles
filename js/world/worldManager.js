var Zamboni = Zamboni || {};
var Engine = Engine || {};

/*
*   Game manager to handle the game logic in the world
*/
Zamboni.World.GameManager = {

    create: function (worldDescriptor) {

        "use strict";

        //The closure with the public methods
        return {

            update: function (delta) {

            }

        };
    }

};
